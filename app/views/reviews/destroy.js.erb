document.querySelector('.flash').innerHTML = ("<%= j render partial: 'shared/flash' %>");

<% unless @review&.persisted? %>
  var review = document.querySelector('#review_<%= @review.id %>');
  review.parentNode.removeChild(review);
  document.querySelector('.review_create_form').style.display = 'block';
  document.querySelector('.game_rate').innerHTML = '<%= @game_stars_partial %>';
<% end %>